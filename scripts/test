#!/usr/bin/env bash

set -euo pipefail

export PYTHONHASHSEED=0 # Avoid non-deterministic issues with sets.
                        # It might make sense to test with a few different seeds
mypy --show-column-numbers --strict ml/pml
mypy --show-column-numbers ml scripts tests
pytest "$@"
